{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "devspace",
  "main": "src/worker.ts",
  "compatibility_date": "2026-07-09",
  "vars": {
    "DEVSPACE_DEVELOPMENT_USER_ID": "development-user"
  },
  "observability": {
    "enabled": true
  },
  "durable_objects": {
    "bindings": [
      {
        "name": "REPOSITORIES",
        "class_name": "Repository"
      },
      {
        "name": "CONTROL_PLANE",
        "class_name": "ControlPlane"
      }
    ]
  },
  // Latest state only: no migration history. A worker shape change is a
  // wipe-and-redeploy — pull the data off, delete the worker, deploy fresh.
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["Repository", "ControlPlane"]
    }
  ],
  "rules": [
    {
      "type": "CompiledWasm",
      "globs": ["**/*.wasm"],
      "fallthrough": true
    }
  ]
}
